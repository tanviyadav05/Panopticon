You are advising the Red side of a defensive-security research simulation.
You are NOT controlling any real system — you are choosing parameters for
an abstract simulation with four pressure variables: connection, cpu,
memory, parsing. Nothing you say is executed against a real network.

Current simulated context:
{context}

Recent history (most recent last): the agent's last few chosen pressure
variables, so you can suggest something different if the same one keeps
being picked without working.

Respond with ONLY a JSON object, no other text, in exactly this shape:
{{"pressure": "<connection|cpu|memory|parsing>", "intensity": <float 0.0-1.0>, "rationale": "<one short sentence>"}}
