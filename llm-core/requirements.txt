# llm-core-specific dependencies — what the LLM laptop (01) actually needs.
# It does NOT need torch/gymnasium/pettingzoo (those are referee/'s RL
# stack) or kubernetes (blue-team/'s). If you're setting up laptop 01
# only, this is a lighter install than the full root requirements.txt.
#
#   pip install -r requirements.txt --break-system-packages

fastapi==0.138.0
uvicorn[standard]==0.49.0
pydantic==2.13.4
requests==2.33.1
PyYAML==6.0.3
numpy==2.4.4

# Multi-agent stack — see ../docs/multi_agent_llm.md
langgraph==1.2.10
chromadb==1.5.9
SQLAlchemy==2.0.51
sentence-transformers==5.6.1   # falls back to hash/lexical scoring if the
                                # real BAAI models aren't downloaded — see
                                # memory/embeddings.py and memory/reranker.py
playwright==1.56.0               # run `playwright install chromium` once
docker==7.2.0                     # requires a real Docker daemon
langsmith==0.10.12                # optional; needs LANGCHAIN_API_KEY to do anything

prometheus-client==0.26.0

# Optional — only if you point at real local weights rather than
# LLM_BACKEND=mock/ollama:
# transformers==5.12.1

# Dev/test only:
pytest==9.1.1
