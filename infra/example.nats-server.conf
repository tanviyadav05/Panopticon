# Not currently used by any code in this repo — see README.md. Kept here as
# a ready-to-go starting point for the day infra/ stops being aspirational.
port: 4222
http_port: 8222

# Bind to the lab switch's subnet, not 0.0.0.0, once you've assigned static
# IPs per deployment/inventory.yaml.
# net: 192.168.50.15
